import random
import re

def roll (roll, data):
    """
    This function allows you to roll dices when you need to.
    "roll" means how many dices you want to throw at once.
    "data" means how many sizes do a single dice have.
    """
    class dice
        num = random.randint(1,100)
print (roll)